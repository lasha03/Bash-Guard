Assignments in command context:
FOO=bar echo "hi"

enigma of environment
declare -- "$1"="$2"

journey of the path

--fix feature

cd $WORKDIR
^---------^ SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.


